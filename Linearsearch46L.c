#include<stdio.h>
#include<stdlib.h>
#include<time.h>

#define INI 10
#define MAX 100
#define INCRE 10

int main(){
    int *a;
    FILE *bestls, *worstls, *avgls;
    int key, count=0, n;

    system("rm bestls.txt");
    system("rm worstls.txt");
    system("rm avgls.txt");

    fopen()

    srand(time(NULL));

    for(n=INI; n<=MAX; n+=INCRE){

    }
}